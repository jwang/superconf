<%= form_for([:admin, @event, @sponsorship_level]) do |f| %>
  <% if @sponsorship_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sponsorship_level.errors.count, "error") %> prohibited this sponsorship level from being saved:</h2>

      <ul>
      <% @sponsorship_level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :maximum %><br />
    <%= f.number_field :maximum %>
  </div>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

